# Proxy

Sub-variants / Notes:

1. Virtual Proxy (lazy loading)
2. Remote Proxy (network access)
3. Protection Proxy (access control)
4. Smart Proxy (extra functionality)

Each subtype folder now includes its own README, code-focused explanation, and UML diagram, and the `tests/` project covers the checklist scenarios for each variant.
